ore Deduplication Configuration
Your system uses the enhanced AlertDeduper from dedup.py with these settings:

Alert Configuration (from dedup.py)
# Power & Performance Alerts
"power_hitter": {
    "window": 15,                    # 15-second duplicate prevention
    "scope": "plate_appearance",     # Per-batter, per-PA tracking
    "content_fields": ["batter_id", "pa_id", "prob_tier"],
    "realert_after_secs": None
}
# Scoring Situation Alerts (Fixed 8/15/2025)
"bases_loaded_no_outs": {
    "window": 60,                    # 1-minute window
    "scope": "plate_appearance",     # Per-batter tracking
    "content_fields": ["bases_hash", "outs", "batter_id"],
    "realert_after_secs": 180        # Re-alert after 3 minutes
}
"runners_23_no_outs": {
    "window": 60,
    "scope": "plate_appearance", 
    "content_fields": ["bases_hash", "outs", "batter_id"],
    "realert_after_secs": 180
}
# Play-by-Play Alerts
"hit": {
    "window": 15,
    "scope": "play",                 # Per individual play
    "content_fields": ["play_id", "description"],
    "realert_after_secs": None
}
"home_run": {
    "window": 15,
    "scope": "play",
    "content_fields": ["play_id", "description"], 
    "realert_after_secs": None
}
Deduplication System Features
Token Bucket Settings (from mlb_monitor.py):

Capacity: 8 tokens per game
Refill Rate: 15-second refill period
Burst Control: Enabled to prevent alert flooding
Memory Management:

Cleanup Frequency: Every 20 monitoring iterations (~1 minute)
Retention: Variable TTL by alert type (15s - 300s + 10s buffer)
Thread Safety: Full RLock protection
Scoping System:

plate_appearance: Power hitter, scoring situations (per-batter)
play: Hits, home runs (per-discrete-event)
half_inning: Persistent conditions
game: Weather, delays (per-game)
Current Status
✅ Enhanced deduplication system active (v2)
✅ Persistent state saving/restoration enabled
✅ 19 alert types configured
✅ Thread-safe operation with monotonic time
✅ Automatic cleanup prevents memory leaks
The system prevents duplicate alerts while ensuring meaningful events are captured through intelligent scoping and timing controls.